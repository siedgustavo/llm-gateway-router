services:
  llama-qwen3coder:
    image: ${LLAMACPP_SERVER_IMAGE:-evilfreelancer/llama.cpp-rpc:latest}
    container_name: aiworker-llama-qwen3coder
    restart: unless-stopped
    entrypoint: ["/bin/bash"]
    command:
      - -lc
      - >
        cd /usr/lib/x86_64-linux-gnu;
        for lib in libllama libggml libggml-base libggml-cpu libggml-rpc libmtmd; do
          [ -e "$$lib.so.0" ] || ln -s "$$lib.so" "$$lib.so.0";
        done;
        exec /app/llama-server --host 0.0.0.0 --port 8080
        --cache-ram 0
        --ctx-checkpoints 0
        -m ${QWEN3CODER_GGUF:-/models/qwen3coder-35b.gguf}
        -ngl ${QWEN3CODER_GPU_LAYERS:-99}
        --ctx-size ${QWEN3CODER_CTX_SIZE:-8192}
        --rpc ${QWEN3CODER_RPC_BACKENDS:-octoserver.core.sied.ar:5000}
    volumes:
      - ${MODELS_DIR:-/opt/llamacpp-rpc/models}:/models:ro
    ports:
      - "${QWEN3CODER_PORT:-8080}:8080"

  llama-qwen36-uncensored:
    # PINNED a b8857 (commit a6cc43c, ultimo "bueno" antes de la regresion CUDA
    # de MoE en issue ggml-org/llama.cpp#24937 -- crashea con este modelo). Debe
    # coincidir con la imagen del RPC backend (llamacpp-rpc-gpu1 en octoserver).
    image: ghcr.io/siedgustavo/llama-cpp-rpc:b8857
    container_name: aiworker-llama-qwen36-uncensored
    restart: unless-stopped
    entrypoint: ["/bin/bash"]
    command:
      - -lc
      - >
        cd /usr/lib/x86_64-linux-gnu;
        for lib in libllama libggml libggml-base libggml-cpu libggml-rpc libmtmd; do
          [ -e "$$lib.so.0" ] || ln -s "$$lib.so" "$$lib.so.0";
        done;
        exec /app/llama-server --host 0.0.0.0 --port 8080
        --cache-ram 0
        --ctx-checkpoints 0
        -m ${QWEN36_UNCENSORED_GGUF:-/models/qwen3.6-uncensored.gguf}
        -ngl ${QWEN36_UNCENSORED_GPU_LAYERS:-99}
        --ctx-size ${QWEN36_UNCENSORED_CTX_SIZE:-8192}
        --rpc ${QWEN36_UNCENSORED_RPC_BACKENDS:-octoserver.core.sied.ar:5001}
    volumes:
      - ${MODELS_DIR:-/opt/llamacpp-rpc/models}:/models:ro
    ports:
      - "${QWEN36_UNCENSORED_PORT:-8081}:8080"

  llama-llama31-pro:
    image: ${LLAMACPP_SERVER_IMAGE:-evilfreelancer/llama.cpp-rpc:latest}
    container_name: aiworker-llama-llama31-pro
    restart: unless-stopped
    entrypoint: ["/bin/bash"]
    command:
      - -lc
      - >
        cd /usr/lib/x86_64-linux-gnu;
        for lib in libllama libggml libggml-base libggml-cpu libggml-rpc libmtmd; do
          [ -e "$$lib.so.0" ] || ln -s "$$lib.so" "$$lib.so.0";
        done;
        exec /app/llama-server --host 0.0.0.0 --port 8080
        --cache-ram 0
        --ctx-checkpoints 0
        -m ${LLAMA31_PRO_GGUF:-/models/llama3.1-pro.gguf}
        -ngl ${LLAMA31_PRO_GPU_LAYERS:-99}
        --ctx-size ${LLAMA31_PRO_CTX_SIZE:-8192}
        --rpc ${LLAMA31_PRO_RPC_BACKENDS:-octoserver.core.sied.ar:5002}
    volumes:
      - ${MODELS_DIR:-/opt/llamacpp-rpc/models}:/models:ro
    ports:
      - "${LLAMA31_PRO_PORT:-8082}:8080"

  llama-playground:
    image: ${LLAMACPP_SERVER_IMAGE:-evilfreelancer/llama.cpp-rpc:latest}
    container_name: aiworker-llama-playground
    restart: unless-stopped
    profiles: ["playground"]
    entrypoint: ["/bin/bash"]
    command:
      - -lc
      - >
        cd /usr/lib/x86_64-linux-gnu;
        for lib in libllama libggml libggml-base libggml-cpu libggml-rpc libmtmd; do
          [ -e "$$lib.so.0" ] || ln -s "$$lib.so" "$$lib.so.0";
        done;
        exec /app/llama-server --host 0.0.0.0 --port 8080
        --cache-ram 0
        --ctx-checkpoints 0
        -m ${PLAYGROUND_GGUF:-/models/playground.gguf}
        -ngl ${PLAYGROUND_GPU_LAYERS:-99}
        --ctx-size ${PLAYGROUND_CTX_SIZE:-8192}
        --rpc ${PLAYGROUND_RPC_BACKENDS:-octoserver.core.sied.ar:5003}
    volumes:
      - ${MODELS_DIR:-/opt/llamacpp-rpc/models}:/models:ro
    ports:
      - "${PLAYGROUND_PORT:-8083}:8080"
